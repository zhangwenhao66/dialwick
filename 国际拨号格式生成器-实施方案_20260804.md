# DialWick 国际拨号格式生成器 —— 实施方案

**背景**：同 WageLark 那份文档——2026-08-04 调研发现单一事实查询页对 AI Overview 最脆弱，DialWick 现有的区号/国际拨号页面基本都是这种格式，这个工具是给其中风险最高的国际拨号板块加一个真正需要用户输入才能出结果的功能。

**原两个方案都已否决**：
- "两地时差计算器"：`src/lib/`/`src/lib/data/` 里没有任何时区数据，需要新补一份区号→时区的映射表，本身不是复用现有数据，工作量比预期大
- "国际通话费用估算器"：运营商/套餐定价没有统一权威数字，做出来是编造精确度不存在的数据，违反项目硬规则

**现方案：国际拨号格式生成器**。数据 100% 复用 `src/lib/internationalDialing.ts` 里已有的 10 个国家规则，零新增数据源。

## 功能描述

用户在 `/international-dialing/[slug]` 某个国家的页面上，输入自己在该国的本地号码（比如英国号码 "020 7946 0958"），工具自动：

1. 按该国家已有的 `exitCode` / `format` / `quirk` 规则，去除本地写法里的干扰字符（空格、括号、连字符）和该国特有的国内长途前缀（比如英国要去掉开头的 trunk prefix "0"，这个规则已经写在 `quirk` 字段里，是自然语言描述不是结构化规则——需要针对现有 10 个国家各自把这条描述转成可执行的格式化逻辑）
2. 拼出完整的、可直接拨打的国际格式号码：`exitCode + countryCode + 处理过的本地号码`
3. 同时展示 `plusWorks` 为 true 时的"+"号替代写法（手机/VoIP 场景），跟 `exitCode` 写法并列展示
4. 结果下方复述该国的 `quirk` 说明，让用户理解"为什么号码被这样改写"，不是黑箱吐出一个数字

## 数据与实施

数据源：`src/lib/internationalDialing.ts` 的 `dialingGuides: DialingGuide[]`，10 个国家已有 `slug`/`country`/`countryCode`/`exitCode`/`plusWorks`/`format`/`example`/`quirk` 字段。

**关键工作**：`quirk` 目前是给读者看的自然语言描述，不是给代码用的结构化规则。需要为每个国家写一个格式化函数（或者一个统一函数 + 每国的参数化配置），把"去掉开头的0"这类规则变成可执行代码。逐个核对现有 10 个国家的 `quirk` 文本，正确处理每个国家的具体规则（不要凭猜测统一处理，比如 Mexico 那条已经写明"2019年改革后手机号不再有额外前缀"，跟 UK 的"去掉trunk prefix 0"是不同规则，不能用同一段代码硬套）。处理不了的边界输入（比如用户输入的号码位数明显不对）要给出清晰的错误提示，不能悄悄吐出一个错误号码。

1. `src/lib/dialFormatter.ts` —— 纯函数 `formatForDialing(localNumber: string, guide: DialingGuide): { exitCodeFormat: string, plusFormat: string, warning?: string }`，附带单元测试 `dialFormatter.test.ts`，覆盖全部 10 个国家的至少一个真实号码样例（可以直接用现有 `example` 字段反推测试用例：把 `example` 里的国家部分去掉、加工出对应的"用户输入"，验证格式化后能还原出等价的 `example`）。
2. 在 `src/pages/international-dialing/[slug].astro` 页面加一个新组件（`src/components/DialFormatBuilder.astro`），文本输入框 + 按钮，vanilla `<script type="module">` 调用 `dialFormatter.ts`（不新增框架依赖，理由同 WageLark 那份文档）。
3. 该国家的 `DialingGuide` 对象作为 props 静态注入组件。

## 验收标准

- `npm test` 全绿，10 个国家的测试用例全部通过
- `npm run build` 无错误
- 手动验证至少 3 个规则明显不同的国家（比如 Mexico/UK/Canada，三者的 `quirk` 规则彼此都不一样）
- 完成后 `git commit` + `push`，Cloudflare Pages 自动部署，轮询新页面确认线上生效
- 仓库根日志追加记录说明这是产品功能上线
